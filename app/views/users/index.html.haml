%h1 Listing users

%table
  %tr
    %th Name
    %th Github name
    %th Icon
    %th

  - @users.each do |user|
    %tr
      %td= user.name
      %td= user.github_name
      %td
        %img{:src => user.icon_url}
      %td
        %ul.button-bar
          %li.first.last
            = link_to 'Detail', user
